spring:
  profiles:
    active: dev

  security:
    filter:
      order: 10

  mvc:
    async:
      request-timeout: 15000

server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    disable-swagger-default-url: true